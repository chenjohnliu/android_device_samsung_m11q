r_dir_file(hal_bluetooth_qti, vendor_convergence_data_file)

allow hal_bluetooth_qti efs_file:dir search;
allow hal_bluetooth_qti bluetooth_efs_file:dir search;
allow hal_bluetooth_qti bluetooth_efs_file:file { open read write append getattr };

set_prop(hal_bluetooth_qti, vendor_bluetooth_prop)
